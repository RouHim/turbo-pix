model: github_copilot::openai/gpt-5.4-mini
reasoning_effort: "high"
budget:
  max_time_minutes: 120
commands:
  test: cargo test
  lint: cargo clippy --message-format=json
  build: cargo build
  audit: cargo audit --quiet --json
git:
  branch_prefix: nighthawk/
  pr_tool: gh
