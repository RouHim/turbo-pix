services:
  turbopix:
    build:
      context: .
      dockerfile: container/Containerfile
    ports:
      - "18473:18473"
    volumes:
      - ./data:/data
      - /home/rouven/Pictures:/photos:ro
    environment:
      - RUST_LOG=info
      - TURBO_PIX_HOST=0.0.0.0
    restart: unless-stopped